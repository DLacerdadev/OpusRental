VERDICT: REJECTED
Primary conclusion: the feature is close, but there is a blocking backend authorization integration gap that will prevent the new endpoint from functioning in this codebase’s current auth model.

Critical findings:
- **Blocking:** `POSVERDICT: REJECTED
T /api/rental-contracts/:id/generate-invoice` was added in `server/routes.tPrimary conclusion: the feature is close, but there is a blocking backend authorization integration gap that will prevent the new endpoint from functioning in this codebase’s current auth model.
s`, but no corresponding policy entry was added in `server/po
licies.ts`. The `authorize()` middleware depends onCritical findings:
 `matchPolicy`; when a route is missing, it returns `404 Not found` before business logic runs. So the new button can call the endpoint, but generation will fail at auth/policy layer.
- Reuse of generation logic is correctly implemented: route calls `InvoiceAutomationService.generateInvoiceForContract(...)` (good, no duplicated dueDate/referenceMonth logic).
- Duplicate handling is implemented correctly at API contract level: duplicate returns `409` and client maps it to specific duplicate toast.
- Client UX requirements are mostly satisfied: manager/admin + active-only visibility, confirmation dialog, loading state, success/error toasts, and invalidation of `['/api/invoices']`.
- i18n keys were added in both `pt-BR` and `en-US`.

Security:
- None observed at serious threshold. Tenant scoping and role checks are present in route logic.

Next actions:
1. Add explicit policy mapping(s) for the new endpoint in `server/policies.ts` (at minimum `POST /api/rental-contracts/:id/generate-invoice` for manager/admin) so `authorize()` allows execution.
2. Run a quick manual smoke test after policy update: first click creates invoice (201), second click same month returns 409 and duplicate toast.
3. Optional polish: if product explicitly requires visible text button (not icon-only), render label in actions column or ensure tooltip/accessibility spec is accepted.- **Blocking:** `POST /api/rental-contracts/:id/generate-invoice` was added in `server/routes.ts`, but no corresponding policy entry was added in `server/policies.ts`. The `authorize()` middleware depends on `matchPolicy`; when a route is missing, it returns `404 Not found` before business logic runs. So the new button can call the endpoint, but generation will fail at auth/policy layer.
- Reuse of generation logic is correctly implemented: route calls `InvoiceAutomationService.generateInvoiceForContract(...)` (good, no duplicated dueDate/referenceMonth logic).
- Duplicate handling is implemented correctly at API contract level: duplicate returns `409` and client maps it to specific duplicate toast.
- Client UX requirements are mostly satisfied: manager/admin + active-only visibility, confirmation dialog, loading state, success/error toasts, and invalidation of `['/api/invoices']`.
- i18n keys were added in both `pt-BR` and `en-US`.

Security:
- None observed at serious threshold. Tenant scoping and role checks are present in route logic.

Next actions:
1. Add explicit policy mapping(s) for the new endpoint in `server/policies.ts` (at minimum `POST /api/rental-contracts/:id/generate-invoice` for manager/admin) so `authorize()` allows execution.
2. Run a quick manual smoke test after policy update: first click creates invoice (201), second click same month returns 409 and duplicate toast.
3. Optional polish: if product explicitly requires visible text button (not icon-only), render label in actions column or ensure tooltip/accessibility spec is accepted.